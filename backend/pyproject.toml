[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-m 'not browser'"
markers = [
  "browser: requires a local Chromium process and is excluded from the default unit-test run.",
]
